{
  "name": "cribbage-pwa",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun --hot dev.ts",
    "build": "bun run build.ts",
    "test": "bun test",
    "fmt": "prettier --write .",
    "fmt:check": "prettier --check .",
    "ci": "bun run fmt:check && bun test && bun run build"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "prettier": "^3.8.3"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "cribbage-counter": "^1.0.4",
    "typedeck": "^1.5.2"
  }
}
